# PersonaPlex shim configuration
PERSONAPLEX_HOST=127.0.0.1
PERSONAPLEX_PORT=9160
PERSONAPLEX_MODEL=personaplex

# Option A: proxy to an upstream server that already speaks OpenAI-compatible chat
# PERSONAPLEX_UPSTREAM_BASE_URL=http://127.0.0.1:9161

# Option B: run a command per request (reads PERSONAPLEX_REQUEST_JSON from env)
# PERSONAPLEX_RUN_COMMAND=python3 scripts/run_personaplex.py
# PERSONAPLEX_WORKDIR=/var/lib/personaplex/app

PERSONAPLEX_TIMEOUT_SEC=120
PERSONAPLEX_DEVICE=cuda
