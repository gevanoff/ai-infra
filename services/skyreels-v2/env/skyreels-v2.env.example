# SkyReels-V2 shim configuration
SKYREELS_HOST=0.0.0.0
SKYREELS_PORT=9190

# Option A: proxy to an upstream HTTP server
# SKYREELS_UPSTREAM_BASE_URL=http://127.0.0.1:9191

# Option B: run a command per request
# SKYREELS_RUN_COMMAND=/var/lib/skyreels-v2/venv/bin/python3 /var/lib/skyreels-v2/tools/run_skyreels_cli.py
# SKYREELS_WORKDIR=/var/lib/skyreels-v2/app

SKYREELS_TIMEOUT_SEC=3600
SKYREELS_MAX_CONCURRENCY=1
SKYREELS_OUT_DIR=/var/lib/skyreels-v2/out
SKYREELS_DEVICE=cuda

# Install-time (optional): PyTorch wheel index configuration
# These are read by `scripts/install.sh` when you run it (from your shell env).
# They're listed here for convenience if you keep a single canonical env file.
#
# Fleet-wide default:
# AI_TORCH_INDEX_URL=https://download.pytorch.org/whl/cu121
# AI_TORCH_INDEX_URL=https://download.pytorch.org/whl/cpu
# AI_TORCH_EXTRA_INDEX_URL=
#
# Per-service override (wins over AI_TORCH_*):
# SKYREELS_TORCH_INDEX_URL=
# SKYREELS_TORCH_EXTRA_INDEX_URL=
