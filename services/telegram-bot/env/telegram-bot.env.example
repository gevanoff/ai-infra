# Telegram Bot Configuration
# Copy this file to /var/lib/telegram-bot/telegram-bot.env and customize.
# Use plain KEY=VALUE lines (no export needed).

# Required: Telegram bot token from @BotFather
TELEGRAM_TOKEN=your_telegram_token_here

# Required: Bearer token for gateway authentication
GATEWAY_BEARER_TOKEN=your_gateway_bearer_token_here

# Optional: Gateway port (defaults to 8800). Host is always 127.0.0.1 and HTTPS is required.
GATEWAY_PORT=8800

# Optional: Model to use (defaults to 'auto')
GATEWAY_MODEL=auto

# Optional: System prompt prepended to each chat
SYSTEM_PROMPT=

# Optional: Maximum number of messages to keep in history (defaults to 20)
# Note: This is total messages, not pairs. System prompts are kept separately.
MAX_HISTORY=20

# Optional: Logging controls
# LOG_LEVEL=info
# LOG_PREVIEW_CHARS=320

# Optional: Max Telegram message size before chunking
# TELEGRAM_MAX_MESSAGE=3900

# Optional: Node TLS handling for self-signed gateway certs
# NODE_OPTIONS=--use-system-ca
# NODE_TLS_REJECT_UNAUTHORIZED=0
