# Template environment for SERVICE_NAME
# Copy/merge into /var/lib/SERVICE_NAME/SERVICE_NAME.env via configure.sh

# Example endpoint the shim will call.
SERVICE_BACKEND_URL=http://127.0.0.1:9000

# Optional: override the shim listen port.
SHIM_PORT=9001

# Optional: log level.
LOG_LEVEL=info
