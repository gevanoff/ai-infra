# Example environment values for Nexa image server.
#
# launchd does not automatically read .env files.
# If Nexa needs env vars, copy them into the plist under EnvironmentVariables.
#
# NEXA_CACHE_DIR=/var/lib/nexa/cache
# NEXA_MODEL_DIR=/var/lib/nexa/models
# NEXA_HOST=127.0.0.1:18181
# NEXA_KEEPALIVE_SEC=600
